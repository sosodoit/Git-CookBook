# 원격 저장소 연결 해제

현재 폴더와 연결된 원격 저장소(origin)를 끊고 싶을 때 사용합니다.

## 상황

다른 원격 저장소로 바꾸거나, 로컬만 사용할 때 원격 연결을 제거하는 경우입니다.

## 명령어

```bash
git remote rm origin
```

## 참고

- 완전히 Git 추적을 없애려면 프로젝트 폴더 안의 숨김 폴더 `.git`을 삭제하면 됩니다. (주의: 로컬 Git 이력이 모두 사라집니다.)
